<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="solid.icon.english">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:name=".architecture.room.App"
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".words_by_levels.universal_topic_level.EnglishLevel" android:screenOrientation="portrait"/>
        <activity android:name=".words_by_levels.study_way.MainStudyAction" android:screenOrientation="portrait"/>
        <activity android:name=".user_line.UserLevel" android:screenOrientation="portrait"/>
        <activity android:name=".user_line.studying.StudyActivity" android:screenOrientation="portrait"/>

        <activity android:name=".navigation_menu.account.AccountActivity" android:screenOrientation="portrait"/>
        <activity android:name=".navigation_menu.settings.SettingsActivity" android:screenOrientation="portrait"/>

        <service android:name=".architecture.notification.FirebaseService"
            android:permission="com.google.android.c2dm.permission.SEND"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>

    </application>

</manifest>