<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="colorPrimaryDark">@color/colorBut</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- To change action bar text color -->
        <item name="android:textColorPrimary">@color/colorBut</item>

        <!-- This has to be here to make it work; let me know if you know why -->
        <item name="actionBarTheme">@style/ThemeOverlay.AppCompat.ActionBar</item>
    </style>

    <style name="AppTabLayout" parent="Widget.Design.TabLayout">
<!--        <item name="tabMaxWidth">60dp</item>-->
        <item name="tabIndicatorColor">?attr/colorAccent</item>
        <item name="tabBackground">@color/colorPrimaryDark</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
<!--        <item name="tabBackground">?attr/selectableItemBackground</item>-->
        <item name="tabTextAppearance">@style/AppTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/back_topic</item>
    </style>

    <!-- for text -->
    <style name="AppTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/back_true</item>
        <item name="textAllCaps">false</item>
    </style>


    <style name="MyCardViewStyle" parent="Theme.AppCompat.Light">
        <item name="cardCornerRadius">6dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardMaxElevation">4dp</item>
        <item name="cardForegroundColor">@color/colorBlack</item>
        <item name="contentPaddingBottom">10dp</item>
        <item name="contentPaddingTop">10dp</item>
        <item name="contentPaddingLeft">5dp</item>
        <item name="contentPaddingRight">5dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="cardBackgroundColor">@color/colorGrayLight</item>
    </style>

</resources>